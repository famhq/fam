@require '../../vars'

.z-weekly-raffle
  flex: 1
  overflow: auto
  -webkit-overflow-scrolling: touch
  padding: 16px

  > .g-grid
    > .subhead
      font-subhead()
      color: getColor('$primary500')
