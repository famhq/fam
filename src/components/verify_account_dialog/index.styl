@require 'zorium-paper/base'

@require '../../vars'

.z-verify-account-dialog_dialog
  display: block

  > .content

    > .error
      font-title()
      color: getColor('$primary500')

    > .input
      margin: 16px 0
      display: flex
      align-items: center

      > .hi
        width: 100px

      > .hyphen
        padding: 8px

    > .text
      font-body1()
