@require 'zorium-paper/base'

@require '../../vars'

.z-edit-guide
  flex: 1
  display: flex
  flex-direction: column

  > .g-grid
    flex: 1
    display: flex
    flex-direction: column

    > .content
      flex: 1
      display: flex
      flex-direction: column
