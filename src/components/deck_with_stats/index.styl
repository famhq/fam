@require 'zorium-paper/base'

@require '../../vars'

.z-deck-with-stats
  width: 100%

  > .stats
    display: flex
    margin-top: 8px

    > .stat
      flex: 1

      > .name
        font-caption1()
        color: getColor('$white54')

      > .value
        font-title()
        margin-top: 4px
