@require 'zorium-paper/base'

@require '../../vars'

.z-deck-cards
  max-width: 600px

  > .row
    margin: -4px 0
    clearfix()

    > .card
      float: left
      padding: 4px
      box-sizing: border-box
