@require '../../vars'

.z-recruiting
  display: flex
  flex-direction: column

  > .welcome
    padding: 16px

    a
      color: getColor('$primary500')

  > .fab
    position: fixed
    bottom: 16px
    right: 16px
    z-index: 50
