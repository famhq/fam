@require 'zorium-paper/base'

@require '../../vars'

.z-stripe-dialog
  > .form
    > .payment-errors
      font-caption1()
      color: getColor('$red500')

    > .form-row
      margin: 8px 0

      &.flex
        display: flex
        align-items: baseline

        > .slash
          margin: 0 16px
