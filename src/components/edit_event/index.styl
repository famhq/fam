@require 'zorium-paper/base'

@require '../../vars'

.z-edit-event
  flex: 1
  display: flex
  flex-direction: column

  > .g-grid
    flex: 1
    display: flex

    > .content
      flex: 1
      overflow: auto
      -webkit-overflow-scrolling: touch
