@require 'zorium-paper/base'

@require '../../vars'

.z-join-group-dialog_dialog
  display: block

  > .content

    > .error
      font-title()
      color: getColor('$primary500')

    > .text
      font-body1()
