@require 'normalize-styl/normalize.styl'
@require 'zorium-paper/base'

@require '../../vars'

.p-decks
  display: flex
  flex-direction: column

  > .fab
    position: fixed
    bottom: 16px
    right: 16px
    z-index: 50
