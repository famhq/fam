@require 'zorium-paper/base'

@require '../../vars'

json('zorium-paper/colors.json')

$zIndexFindFriends = 99999

.p-friends
  display: flex
  flex-direction: column

  > .fab
    position: fixed
    bottom: 16px
    right: 16px
    z-index: 50

  > .find-friends
    position: fixed
    z-index: $zIndexFindFriends
    top: 0
    left: 0
    width: 100%
    height: 100%
